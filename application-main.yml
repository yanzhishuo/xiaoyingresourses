server:
  port: 9989
spring:
  application:
    name: service-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yanzhishuo/xiaoyingresourses.git  # 填写你自己的github的仓库名称       #这个uri用ssh地址一直都是无法访问，只有改成https地址加privateKey或者显示声明账号密码才有用
          username: yanzhishuo
          password: yzs19951208
          search-paths:
            - xiaoyingresourses  #搜索目录
      label: main

  datasource:
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://111.195.185.99:3306/ejs?characterEncoding=utf-8&serverTimezone=UTC
    type: com.alibaba.druid.pool.DruidDataSource
    mvc:
      format:
        date: yyyy-MM-dd
      hiddenmethod:
        filter:
          enabled: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/


