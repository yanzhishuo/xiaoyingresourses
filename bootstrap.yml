spring:
  cloud:
    #Config客户端配置
    config:
      label: main #分支名称
      name: application
      #配置文件名称
      profile: main  #读取后缀名称 上述3个综合： master分支上的config-dev.yml的配置文件被读取http://confi-3344.com:3344/master/config-dev.yml
      uri: http://localhost:9090  #配置中心地址

